# AGENTS.override.md

## Frontend rules

- Keep feature-specific UI under `src/app/features/<domain>/<module>/components`.
- Keep React Three Fiber logic in `visualizer/` folders for tree-shaking.
- Prefer Tailwind utility classes over bespoke CSS modules.
