---
phase: 01-browser-conversion-outputs
plan: 02
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - package.json
  - package-lock.json
  - frontend/src/app/features/tools/step-converter/workers/stepConverter.worker.ts
  - frontend/src/app/features/tools/step-converter/hooks/useStepConverterBrowser.ts
  - frontend/src/app/features/tools/step-converter/components/StepConverterForm.tsx
  - frontend/src/app/features/tools/step-converter/components/StepConverterOutputs.tsx
  - frontend/src/app/features/tools/step-converter/utils.ts
  - frontend/src/app/features/tools/step-converter/types.ts
  - frontend/src/app/i18n/translations.ts
autonomous: true

must_haves:
  truths:
    - 'User can select a STEP file (<= 15MB) and start conversion'
    - 'Conversion runs in a Web Worker with stage-based progress and cancel'
    - 'On success, user downloads a single bundle containing GLB + metadata'
    - 'On failure (invalid/too large/unsupported), user sees an actionable error and can retry'
  artifacts:
    - path: 'frontend/src/app/features/tools/step-converter/workers/stepConverter.worker.ts'
      provides: 'Worker conversion + progress + bundle assembly'
    - path: 'frontend/src/app/features/tools/step-converter/hooks/useStepConverterBrowser.ts'
      provides: 'UI controller: size cap + progress wiring + cancel/retry'
    - path: 'frontend/src/app/features/tools/step-converter/components/StepConverterForm.tsx'
      provides: 'STEP-only file input + cancel button behavior'
    - path: 'frontend/src/app/features/tools/step-converter/components/StepConverterOutputs.tsx'
      provides: 'Bundle download CTA'
  key_links:
    - from: 'frontend/src/app/features/tools/step-converter/hooks/useStepConverterBrowser.ts'
      to: 'frontend/src/app/features/tools/step-converter/workers/stepConverter.worker.ts'
      via: 'postMessage + progress messages'
      pattern: "new URL\\('../workers/stepConverter\\.worker\\.ts'"
    - from: 'frontend/src/app/features/tools/step-converter/workers/stepConverter.worker.ts'
      to: 'opencascade-convert/browser'
      via: 'createConverter + GLB extras injection'
      pattern: "from 'opencascade-convert/browser'"
---

<objective>
Make the browser STEP converter meet Phase 1 requirements: STEP-only, 15MB cap, worker progress/cancel, and a single downloadable bundle containing GLB + assembly metadata.

Purpose: This is the user-facing deliverable for Phase 1.
Output: `/tools/step-converter-browser` produces a `.zip` bundle with `{base}.glb` + `{base}.metadata.json`, and the GLB also embeds the metadata in `asset.extras`.
</objective>

<execution_context>
@/Users/ilker/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ilker/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-browser-conversion-outputs/01-CONTEXT.md

@frontend/src/app/pages/tools/StepConverterBrowserPage.tsx
@frontend/src/app/features/tools/step-converter/hooks/useStepConverterBrowser.ts
@frontend/src/app/features/tools/step-converter/workers/stepConverter.worker.ts
@packages/opencascade-convert/src/core/types.ts
</context>

<tasks>

<task type="auto">
  <name>Add ZIP bundling dependency and implement worker-side bundle creation</name>
  <files>
package.json
package-lock.json
frontend/src/app/features/tools/step-converter/workers/stepConverter.worker.ts
  </files>
  <action>
Add a small browser-safe ZIP creator dependency (recommend `fflate`) to the workspace `package.json`.

Update `frontend/src/app/features/tools/step-converter/workers/stepConverter.worker.ts` to:

- Send progress updates as stages (at minimum: `parsing`, `meshing`, `writing`, `metadata`, `packaging`).
- Convert STEP -> GLB (keep existing triangulation options).
- Always generate assembly metadata for GLB outputs:
  - `nodeMap`: from OCCT document.
  - `bom`: export MUST be simplified to "name + quantity" (per `01-CONTEXT.md`). Keep `productId`/`kind` if already present, but do not require instance lists.
  - Wrap into a single metadata object with a required top-level `schemaVersion` string (e.g. `"bunlar-assembly-metadata@1"`).
- Inject the metadata into the GLB using the helper from Plan `01-01` (as `asset.extras`).
- Build a ZIP bundle (in the worker) containing:
  - `{base}.glb`
  - `{base}.metadata.json` (the same object that was embedded)
    Where `{base}` comes from the input filename without extension (pass filename as part of the worker request).
- Return a single success message with:
  - `bundleName` (e.g. `{base}.zip`)
  - `bundleBytes` (ArrayBuffer)

Error handling:

- Ensure thrown errors propagate as a single failure message with a stable `error` string.
- No partial outputs on error.
  </action>
  <verify>npm install</verify>
  <done>`fflate` (or chosen lib) is installed and the worker can create an in-memory zip buffer.</done>
  </task>

<task type="auto">
  <name>Wire UI controller for STEP-only + 15MB cap + progress + bundle download</name>
  <files>
frontend/src/app/features/tools/step-converter/hooks/useStepConverterBrowser.ts
frontend/src/app/features/tools/step-converter/components/StepConverterForm.tsx
frontend/src/app/features/tools/step-converter/components/StepConverterOutputs.tsx
frontend/src/app/features/tools/step-converter/utils.ts
frontend/src/app/features/tools/step-converter/types.ts
frontend/src/app/i18n/translations.ts
  </files>
  <action>
Implement the Phase 1 UX decisions in the browser converter:

STEP-only + size cap:

- Accept only `.step,.stp` in `StepConverterForm`.
- In `useStepConverterBrowser`, enforce a hard cap of 15MB on file selection (block before conversion starts).
- If cap violated, do not start conversion; show an actionable error and leave the previous state clean.

Progress + cancel:

- Update worker messaging in `useStepConverterBrowser` to listen for progress stage events.
- Display stage-based status text (update `tools.stepConverter.status.loading` to be stage-aware or add new keys like `tools.stepConverter.status.stage.parsing`, etc.).
- Cancel must:
  - terminate the worker immediately,
  - clear any in-flight state and downloads,
  - keep the file selected so user can retry.

Outputs:

- Replace the current single-file download link (for GLB conversions) with a single bundle download link for `{base}.zip`.
- Do not show separate BOM/node-map download links for the browser flow; the bundle must contain metadata sidecar already.

Actionable errors:

- Add translations for: unsupported extension (non-STEP), size cap violation, conversion failure.
- Ensure errors are retryable (no need to reload page).

Keep the existing Advanced options UI where feasible, but Phase 1 must work in default settings without toggles.
</action>
<verify>npx nx run @bunlar/frontend:build</verify>
<done>`@bunlar/frontend` builds; UI shows stage progress; cancel stops conversion and user can retry with the same file.</done>
</task>

<task type="auto">
  <name>Run automated checks for regression safety</name>
  <files>package.json</files>
  <action>
Run the fast checks to ensure changes did not break the workspace:
- `npx nx run opencascade-convert:test`
- `npx nx run opencascade-convert:build`
- `npx nx run @bunlar/frontend:build`
  </action>
  <verify>npx nx run opencascade-convert:test && npx nx run opencascade-convert:build && npx nx run @bunlar/frontend:build</verify>
  <done>All commands pass.</done>
</task>

</tasks>

<verification>
- `npx nx run @bunlar/frontend:build`
- Manual sanity (non-blocking): `npx nx run @bunlar/frontend:dev` then visit `/tools/step-converter-browser`
</verification>

<success_criteria>

- STEP-only file input; files > 15MB are blocked before conversion starts.
- Worker conversion shows stage-based progress and supports cancel without outputs.
- Successful GLB conversion produces a single zip bundle containing GLB + metadata JSON; GLB embeds the same metadata in `asset.extras`.
- Errors are actionable and retryable.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-browser-conversion-outputs/01-02-SUMMARY.md`
</output>
