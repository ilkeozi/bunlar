---
phase: 01-browser-conversion-outputs
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/opencascade-convert/src/occt/glb-metadata.ts
  - packages/opencascade-convert/src/index.ts
  - packages/opencascade-convert/src/browser/index.ts
  - packages/opencascade-convert/src/__tests__/glb-metadata.test.ts
autonomous: true

must_haves:
  truths:
    - 'Browser STEP converter can embed assembly metadata into the produced GLB under `asset.extras`'
    - 'Produced GLB remains a valid, readable GLB after metadata injection (no corruption; standard GLB parsers can still load it)'
    - 'Browser pipeline can call the helper via `opencascade-convert/browser` (no Node-only APIs in the helper surface)'
  artifacts:
    - path: 'packages/opencascade-convert/src/occt/glb-metadata.ts'
      provides: 'GLB JSON-chunk editing + asset.extras injection'
    - path: 'packages/opencascade-convert/src/__tests__/glb-metadata.test.ts'
      provides: 'Unit coverage for inject + error handling'
    - path: 'packages/opencascade-convert/src/browser/index.ts'
      provides: 'Browser entry export for helper'
  key_links:
    - from: 'packages/opencascade-convert/src/browser/index.ts'
      to: 'packages/opencascade-convert/src/occt/glb-metadata.ts'
      via: 'named export'
      pattern: "export\\s*\\{[^}]*inject.*Glb[^}]*\\}"
---

<objective>
Add a small, tested helper that injects assembly metadata into a GLB's JSON chunk (as glTF `asset.extras`).

Purpose: Phase 1 requires GLB outputs that carry assembly metadata without separate wiring.
Output: A unit-tested `injectAssetExtrasIntoGlb()` (name can vary, behavior cannot) exported from both node + browser entrypoints.
</objective>

<execution_context>
@/Users/ilker/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ilker/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-browser-conversion-outputs/01-CONTEXT.md

@packages/opencascade-convert/src/occt/gltf-names.ts
@packages/opencascade-convert/src/browser/index.ts
</context>

<tasks>

<task type="auto">
  <name>RED: Write failing unit tests for GLB extras injection</name>
  <files>packages/opencascade-convert/src/__tests__/glb-metadata.test.ts</files>
  <action>
Create tests that define the contract for a helper that:
- Accepts a `Uint8Array` GLB.
- Parses the JSON chunk.
- Merges/sets `json.asset.extras` with a provided JSON-serializable payload (do not drop existing `asset.extras`).
- Writes a valid GLB back (header + chunk lengths correct; output remains a GLB; non-JSON/BIN chunks preserved byte-for-byte).

Test cases (minimum):

- Inject into GLB that has `asset` but no `extras`.
- Inject into GLB that already has `asset.extras` (must merge, not replace).
- Invalid GLB (wrong magic / truncated) throws a clear Error.

Implementation is not written yet; tests must fail.
</action>
<verify>npx nx run opencascade-convert:test</verify>
<done>Tests fail for the missing helper and/or missing exports.</done>
</task>

<task type="auto">
  <name>GREEN: Implement helper and export it from node + browser entrypoints</name>
  <files>
packages/opencascade-convert/src/occt/glb-metadata.ts
packages/opencascade-convert/src/index.ts
packages/opencascade-convert/src/browser/index.ts
  </files>
  <action>
Implement the helper in `packages/opencascade-convert/src/occt/glb-metadata.ts`.

Constraints:

- No new runtime dependencies.
- Use `TextEncoder`/`TextDecoder` (Node 20+ ok) to encode/decode JSON chunk.
- Ensure JSON chunk is padded to 4-byte alignment per GLB spec.
- Preserve non-JSON chunks as-is; only replace the JSON chunk bytes + chunk length.

Export:

- Re-export from `packages/opencascade-convert/src/index.ts`.
- Re-export from `packages/opencascade-convert/src/browser/index.ts`.

Naming:

- Function name is flexible, but must be obvious and stable (recommend `injectAssetExtrasIntoGlb`).
  </action>
  <verify>npx nx run opencascade-convert:test</verify>
  <done>All new tests pass; helper is importable from both entrypoints.</done>
  </task>

<task type="auto">
  <name>REFACTOR: Ensure build passes and API surface is clean</name>
  <files>
packages/opencascade-convert/src/occt/glb-metadata.ts
packages/opencascade-convert/src/index.ts
packages/opencascade-convert/src/browser/index.ts
  </files>
  <action>
Run the build to ensure types/exports are correct.
- Keep the helper pure (no access to OCCT; only byte-level GLB handling).
- Ensure errors include enough context for UI mapping (e.g., "Invalid GLB: missing JSON chunk").
  </action>
  <verify>npx nx run opencascade-convert:build</verify>
  <done>`opencascade-convert` builds and the new export appears in generated `dist` typings.</done>
</task>

</tasks>

<verification>
- `npx nx run opencascade-convert:test`
- `npx nx run opencascade-convert:build`
</verification>

<success_criteria>

- A tested helper can inject/merge `asset.extras` into a GLB JSON chunk.
- The helper is exported for browser usage as `opencascade-convert/browser`.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-browser-conversion-outputs/01-01-SUMMARY.md`
</output>
