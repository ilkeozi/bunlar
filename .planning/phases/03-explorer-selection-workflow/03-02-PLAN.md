---
phase: 03-explorer-selection-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - '03-01'
files_modified:
  - frontend/src/app/features/climate-tech/assembly-viewer/visualizer/AssemblyCanvas.tsx
  - frontend/src/app/features/climate-tech/assembly-viewer/visualizer/AssemblyModel.tsx
  - frontend/src/app/features/climate-tech/assembly-viewer/visualizer/SelectionOutline.tsx
  - frontend/src/app/features/climate-tech/assembly-viewer/visualizer/FitController.tsx
  - frontend/src/app/features/climate-tech/assembly-viewer/utils/sceneIndex.ts
  - frontend/src/app/features/climate-tech/assembly-viewer/utils/fitCamera.ts
autonomous: true

must_haves:
  truths:
    - 'Clicking geometry in 3D selects the frontmost hit and shows a gearbox-style outline highlight'
    - 'Clicking empty space in 3D clears selection'
    - 'Hidden parts are not visible or pickable in 3D'
    - 'Fit camera animates camera position without changing OrbitControls.target'
  artifacts:
    - path: 'frontend/src/app/features/climate-tech/assembly-viewer/visualizer/AssemblyCanvas.tsx'
      provides: 'R3F canvas wiring: pointer-missed clearing + fit controller'
    - path: 'frontend/src/app/features/climate-tech/assembly-viewer/visualizer/AssemblyModel.tsx'
      provides: 'GLB load + mesh indexing + 3D pick -> store selection + scene visibility application'
    - path: 'frontend/src/app/features/climate-tech/assembly-viewer/visualizer/SelectionOutline.tsx'
      provides: 'Gearbox-style selected outline applied to all meshes in selected part'
    - path: 'frontend/src/app/features/climate-tech/assembly-viewer/utils/fitCamera.ts'
      provides: 'Fit-to-bounds math while keeping OrbitControls target'
  key_links:
    - from: 'frontend/src/app/features/climate-tech/assembly-viewer/visualizer/AssemblyCanvas.tsx'
      to: 'frontend/src/app/features/climate-tech/assembly-viewer/state/useAssemblyExplorerStore.ts'
      via: 'Canvas onPointerMissed + fitRequest consumption'
      pattern: 'onPointerMissed|fitRequestId'
    - from: 'frontend/src/app/features/climate-tech/assembly-viewer/visualizer/AssemblyModel.tsx'
      to: 'frontend/src/app/features/climate-tech/assembly-viewer/utils/ocaf.ts'
      via: 'extractOcafEntry(mesh.name) for picking/indexing'
      pattern: 'extractOcafEntry\('
---

<objective>
Wire selection + visibility behavior into the 3D canvas: pick frontmost meshes, apply gearbox-style outlines, enforce non-pickable hidden parts, and implement fit-to-selection/visible.

Purpose: Deliver the 3D-side of EXPL-01..03 so UI interactions have immediate visual feedback and correct picking semantics.
Output: Updated Assembly Viewer canvas/model with selection/visibility/fit behaviors.
</objective>

<execution_context>
@/Users/ilker/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ilker/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-explorer-selection-workflow/03-CONTEXT.md
@.planning/phases/03-explorer-selection-workflow/03-RESEARCH.md

@frontend/src/app/features/climate-tech/assembly-viewer/visualizer/AssemblyCanvas.tsx
@frontend/src/app/features/climate-tech/assembly-viewer/visualizer/AssemblyModel.tsx
@frontend/src/app/features/climate-tech/carbon-aware-motor-assembly/visualizer/GearboxModel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mesh indexing + 3D picking + gearbox outline highlight</name>
  <files>
frontend/src/app/features/climate-tech/assembly-viewer/utils/sceneIndex.ts
frontend/src/app/features/climate-tech/assembly-viewer/visualizer/SelectionOutline.tsx
frontend/src/app/features/climate-tech/assembly-viewer/visualizer/AssemblyModel.tsx
  </files>
  <action>
Implement the 3D-side selection wiring and highlight style.

1. `utils/sceneIndex.ts`

- Add `indexMeshesByOcafEntry(model: THREE.Object3D): Map<string, THREE.Mesh[]>`.
- Traverse once, keep meshes where `mesh.isMesh === true` and `extractOcafEntry(mesh.name)` is non-null.

2. `visualizer/SelectionOutline.tsx`

- Create a component that reads the store's `selectedOcafEntry` and `meshesByOcafEntry`.
- When selection changes, attach a non-pickable outline to every mesh in the selected entry's list.
- Match the carbon-aware gearbox style exactly:
  - `new THREE.EdgesGeometry(mesh.geometry, 35)`
  - `new THREE.LineBasicMaterial({ color: '#7dd3fc', transparent: true, opacity: 1 })`
  - `material.depthTest = false`, `material.depthWrite = false`
  - `outline.name = 'selected-part-outline'`
  - `outline.raycast = () => {}`
  - `outline.scale.setScalar(1.03)`
- Cleanup must remove outlines from previous meshes and dispose geometry/material.

3. Update `visualizer/AssemblyModel.tsx`

- After cloning and centering the model (keep existing light-stripping + `centerModel()` behavior), build `meshesByOcafEntry` using `indexMeshesByOcafEntry(model)` and store it via `useAssemblyExplorerStore.getState().setMeshesByOcafEntry(...)` (or equivalent action you added in 03-01).
- Add `onPointerDown` handler on the rendered model primitive:
  - Ensure the picked object is a `THREE.Mesh`.
  - Extract ocaf entry from `event.object.name` using `extractOcafEntry`.
  - Call store `selectOcafEntry(entry, '3d')`.
  - `event.stopPropagation()` to avoid accidental parent handling.

Constraints:

- No hover selection.
- Keep R3F's default “frontmost hit” behavior (do not implement custom raycasting).
  </action>
  <verify>
  npx nx run @bunlar/frontend:build
  </verify>
  <done>
  3D clicks select an OCAF entry (frontmost hit) and show the gearbox-style outline on all meshes for that part.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Enforce hidden-not-pickable + add fit-to-selection/visible</name>
  <files>
frontend/src/app/features/climate-tech/assembly-viewer/utils/fitCamera.ts
frontend/src/app/features/climate-tech/assembly-viewer/visualizer/FitController.tsx
frontend/src/app/features/climate-tech/assembly-viewer/visualizer/AssemblyCanvas.tsx
frontend/src/app/features/climate-tech/assembly-viewer/visualizer/AssemblyModel.tsx
  </files>
  <action>
1) Hidden parts not pickable in 3D

- In `AssemblyModel.tsx`, subscribe to visibility state (`nodeMap`, `explicitHiddenNodeIds`, `isolateActive`, `selectedNodeId`) and derive an effective hidden set of leaf part OCAF entries.
- Apply it to the scene using the precomputed `meshesByOcafEntry` index:
  - For each `[ocafEntry, meshes]`, set `mesh.visible = false` when hidden; `true` otherwise.
- Do not clear selection when a selected part becomes hidden (hidden-selected stays selected).

2. Clear selection on empty click

- In `AssemblyCanvas.tsx`, set `Canvas onPointerMissed={() => store.selectOcafEntry(null, '3d')}` (or the equivalent clear action) so empty clicks clear selection.

3. Fit-to-bounds without retargeting

- Add `utils/fitCamera.ts` implementing `computeFitPosition({ camera, target, bounds, margin })` using `THREE.Box3.getBoundingSphere()` + FOV math (as in Phase 03 research): move camera position only; keep the target unchanged.
- Add `visualizer/FitController.tsx` mounted inside the Canvas that:
  - Watches `fitRequestId` and `fitRequestMode` from the store.
  - Computes bounds:
    - mode `selection`: bounds of currently selected meshes (from `selectedOcafEntry` -> `meshesByOcafEntry`).
    - mode `visible`: bounds of all currently visible meshes.
  - Animates `camera.position` to the computed fit position over ~300ms with `requestAnimationFrame`.
  - Keeps `controlsRef.current.target` unchanged; calls `controlsRef.current.update()` during/after animation.
- Wire `FitController` into `AssemblyCanvas.tsx` and pass `controlsRef`.

Constraints:

- Fit is triggered only by store fit requests (the toolbar in plan 03-03 will call `requestFit(...)`).
- If bounds are empty (no meshes), no-op.
  </action>
  <verify>
  npx nx run @bunlar/frontend:build
  </verify>
  <done>
  Hidden parts are not visible/pickable, empty 3D clicks clear selection, and fit requests animate camera position while keeping the orbit target unchanged.
  </done>
  </task>

</tasks>

<verification>
- `npx nx run @bunlar/frontend:build`
</verification>

<success_criteria>

- 3D picking selects the frontmost mesh and highlights the corresponding part using the established gearbox outline style.
- Hidden parts are actually invisible in the scene (and therefore unpickable).
- Fit-to-selection / fit-to-visible works without changing `OrbitControls.target`.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-explorer-selection-workflow/03-02-SUMMARY.md`
</output>
