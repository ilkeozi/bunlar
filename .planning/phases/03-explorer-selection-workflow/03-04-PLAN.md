---
phase: 03-explorer-selection-workflow
plan: 04
type: execute
wave: 3
depends_on:
  - '03-02'
  - '03-03'
files_modified: []
autonomous: false

must_haves:
  truths:
    - 'User can select a tree node and see the corresponding geometry highlighted in 3D'
    - 'User can select geometry in 3D and see the corresponding tree node highlighted and auto-revealed'
    - 'User can hide/isolate parts and fit the camera to the current selection (or visible geometry)'
  artifacts:
    - path: 'frontend/src/app/pages/subjects/climate-tech/AssemblyViewerPage.tsx'
      provides: 'Interactive route for manual verification'
  key_links:
    - from: '/subjects/climate-tech/assembly-hierarchy-explorer'
      to: 'frontend/src/app/features/climate-tech/assembly-viewer/state/useAssemblyExplorerStore.ts'
      via: 'UI interactions drive selection/visibility'
      pattern: 'AssemblyExplorer'
---

<objective>
Human verification of the full Explorer Selection Workflow (Phase 03): tree<->3D selection sync, visibility/isolation controls, and fit behavior.

Purpose: The phase success criteria are interaction-based; this confirms the UX matches the locked semantics end-to-end.
Output: Verified Phase 03 behavior (or a concrete list of gaps for closure).
</objective>

<execution_context>
@/Users/ilker/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ilker/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-explorer-selection-workflow/03-CONTEXT.md

@frontend/src/app/pages/subjects/climate-tech/AssemblyViewerPage.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Assembly Explorer selection + visibility workflow (tree + 3D sync, hide/isolate/show-all, fit camera)</what-built>
  <how-to-verify>
  1) Run the app: `npx nx serve frontend`
  2) Visit: `http://localhost:4200/subjects/climate-tech/assembly-hierarchy-explorer`
  3) Upload a small STEP/assembly and wait for the model to be ready.
  4) Tree -> 3D:
     - Click a leaf/part node in the tree.
     - Expected: the corresponding geometry gets the blue outline highlight in 3D.
  5) 3D -> tree:
     - Click a visible part in 3D.
     - Expected: the corresponding tree node becomes selected, its ancestors expand if needed, and it scrolls into view.
     - Click empty space in 3D.
     - Expected: selection clears (no outline, tree selection cleared).
  6) Visibility + isolation:
     - Use the eye icon on a node to hide it (and test hiding an assembly/subtree node).
     - Expected: hidden parts disappear and cannot be selected in 3D; tree label is dimmed.
     - Select a part and click Isolate; then toggle Isolate off.
     - Expected: isolate shows only the selection; turning it off restores the exact prior hide/show configuration.
     - Click Show all.
     - Expected: everything becomes visible; isolation clears; selection stays selected.
  7) Fit:
     - With a selection, click Fit.
     - Expected: camera smoothly moves to frame the selection while keeping the orbit target unchanged.
     - Clear selection and click Fit.
     - Expected: camera frames all visible geometry.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the mismatch (what you did vs what happened).</resume-signal>
</task>

</tasks>

<verification>
- Manual verification steps in the checkpoint task
</verification>

<success_criteria>

- EXPL-01, EXPL-02, EXPL-03 behaviors are confirmed in a real browser session.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-explorer-selection-workflow/03-04-SUMMARY.md`
</output>
