---
phase: 04-decommission-nx-projects
plan: 02
type: execute
wave: 2
depends_on:
  - '04-01'
files_modified:
  - packages/occt-api/project.json
  - package.json
  - package-lock.json
  - tsconfig.json
autonomous: true

must_haves:
  truths:
    - 'Nx project list does not include occt-api (explicit or inferred package name)'
    - 'Workspace config no longer treats packages/occt-api as an active workspace project'
  artifacts:
    - path: 'packages/occt-api/project.json'
      provides: 'Removed Nx project definition for occt-api'
    - path: 'package.json'
      provides: 'Workspaces exclude packages/occt-api so Nx will not infer it as a project'
    - path: 'tsconfig.json'
      provides: 'Root TS project references no longer include packages/occt-api'
  key_links:
    - from: 'package.json'
      to: 'nx project discovery'
      via: 'npm workspaces globs'
      pattern: '"workspaces"'
---

<objective>
Remove `occt-api` from the Nx project graph (and prevent it from being re-discovered via npm workspaces).

Purpose: `occt-api` is being decommissioned; keeping it in Nx creates broken targets and unnecessary graph complexity.
Output: `npx nx show projects` no longer lists `occt-api` (nor an inferred `@bunlar/occt-api`), and the workspace lockfile matches updated workspaces.
</objective>

<execution_context>
@/Users/ilker/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ilker/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-decommission-nx-projects/04-RESEARCH.md

@packages/occt-api/project.json
@packages/occt-api/package.json
@package.json
@package-lock.json
@tsconfig.json
@nx.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove explicit Nx project config for `occt-api`</name>
  <files>packages/occt-api/project.json</files>
  <action>
Delete the explicit Nx project definition at `packages/occt-api/project.json`.

Notes:

- Do not delete the `packages/occt-api/` directory in this plan; deletion happens in Phase 5.
- This is not sufficient on its own (Nx can still infer projects from npm workspaces). Task 2 handles that.
  </action>
  <verify>test ! -f packages/occt-api/project.json</verify>
  <done>`packages/occt-api/project.json` no longer exists in the repo.</done>
  </task>

<task type="auto">
  <name>Task 2: Exclude `packages/occt-api` from npm workspaces and align TypeScript references</name>
  <files>
package.json
package-lock.json
tsconfig.json
  </files>
  <action>
Prevent Nx from inferring `packages/occt-api/package.json` as a workspace project by excluding it from root npm workspaces, then update the lockfile and TS project references.

1. Edit root `package.json` `workspaces` array to exclude `packages/occt-api` (e.g. add `"!packages/occt-api"`).
   - Fallback (if workspace negation is ignored by your tooling): replace the broad glob with an explicit allowlist of workspace paths excluding `packages/occt-api` (keep `packages/*` only if you truly intend to include all packages).
2. Run `npm install` to update `package-lock.json` to match the new workspaces.
3. Remove the `./packages/occt-api` entry from root `tsconfig.json` `references` (so TS build tooling no longer traverses it as part of the workspace composite build).
   </action>
   <verify>npm install && npx nx reset && npx nx show projects --json | node -e "const fs=require('fs'); const p=JSON.parse(fs.readFileSync(0,'utf8')); const bad=['cad-converter','occt-api','@bunlar/occt-api']; const found=bad.filter(x=>p.includes(x)); if (found.length) { console.error('Nx still includes: ' + found.join(', ')); process.exit(1); }" && (npx nx show project occt-api >/dev/null 2>&1; test $? -ne 0) && (npx nx show project @bunlar/occt-api >/dev/null 2>&1; test $? -ne 0) && npx nx graph --file=/tmp/nx-graph.html && test -f /tmp/nx-graph.html</verify>
   <done>
   Root workspaces exclude `packages/occt-api` (lockfile updated), root `tsconfig.json` no longer references it, and Nx no longer lists `occt-api` (explicit) or `@bunlar/occt-api` (inferred).
   </done>
   </task>

</tasks>

<verification>
- `npm install` (lockfile updated for workspace change)
- `npx nx reset`
- `npx nx show projects` (confirm `occt-api` absent)
</verification>

<success_criteria>

- `occt-api` is not an Nx project anymore (no graph node, no targets).
- Nx does not rediscover it via `packages/*` workspaces.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-decommission-nx-projects/04-02-SUMMARY.md`
</output>
