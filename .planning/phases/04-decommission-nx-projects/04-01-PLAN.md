---
phase: 04-decommission-nx-projects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cad-converter/project.json
autonomous: true

must_haves:
  truths:
    - 'Nx project list does not include cad-converter'
  artifacts:
    - path: 'cad-converter/project.json'
      provides: 'Removed Nx project definition for cad-converter'
  key_links:
    - from: 'cad-converter/project.json'
      to: 'nx project graph'
      via: 'Nx project discovery'
      pattern: '"name"\s*:\s*"cad-converter"'
---

<objective>
Remove `cad-converter` from the Nx project graph by deleting its explicit `project.json`.

Purpose: `cad-converter` is a legacy Nx project; removing it simplifies the workspace and prevents broken Nx targets.
Output: `npx nx show projects` no longer lists `cad-converter` and `npx nx show project cad-converter` fails.
</objective>

<execution_context>
@/Users/ilker/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ilker/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-decommission-nx-projects/04-RESEARCH.md

@cad-converter/project.json
@nx.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove `cad-converter` Nx project configuration</name>
  <files>cad-converter/project.json</files>
  <action>
Delete the explicit Nx project definition at `cad-converter/project.json`.

Notes:

- Do not delete the `cad-converter/` directory in this plan; this plan is about Nx project discovery only.
- Do not touch docs/scripts references here; those are handled in Phase 6.
  </action>
  <verify>test ! -f cad-converter/project.json</verify>
  <done>`cad-converter/project.json` no longer exists in the repo.</done>
  </task>

<task type="auto">
  <name>Task 2: Reset Nx graph cache and verify project is gone</name>
  <files>cad-converter/project.json</files>
  <action>
Verify Nx no longer discovers the project.

1. Clear cached graph state.
2. Ensure `cad-converter` is not listed as a project.
3. Ensure `nx show project cad-converter` fails.
   </action>
   <verify>npx nx reset && npx nx show projects --json | node -e "const fs=require('fs'); const p=JSON.parse(fs.readFileSync(0,'utf8')); if (p.includes('cad-converter')) process.exit(1);" && (npx nx show project cad-converter >/dev/null 2>&1; test $? -ne 0)</verify>
   <done>`npx nx show projects` does not list `cad-converter`, and `npx nx show project cad-converter` exits non-zero.</done>
   </task>

</tasks>

<verification>
- `npx nx reset`
- `npx nx show projects` (confirm `cad-converter` absent)
</verification>

<success_criteria>

- `cad-converter` is not an Nx project anymore (no graph node, no targets).
  </success_criteria>

<output>
After completion, create `.planning/phases/04-decommission-nx-projects/04-01-SUMMARY.md`
</output>
