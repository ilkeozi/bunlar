---
phase: 02-mesh-quality-baseline
plan: 4
type: execute
wave: 2
depends_on:
  - '02-01'
  - '02-02'
  - '02-03'
files_modified: []
autonomous: false

must_haves:
  truths:
    - 'User can orbit/pan/zoom converted assemblies smoothly in the Assembly Hierarchy Explorer on a mid-range desktop Chrome'
    - 'Triangle explosion auto-mitigation triggers (when needed) and produces a post-conversion warning that is persisted in outputs'
  artifacts:
    - path: 'frontend/src/app/features/climate-tech/assembly-viewer/visualizer/AssemblyCanvas.tsx'
      provides: 'Dev overlay host'
    - path: 'frontend/src/app/features/tools/step-converter/workers/stepConverter.worker.ts'
      provides: 'Retry-on-explosion + warnings persistence'
  key_links:
    - from: 'frontend/src/app/features/tools/step-converter/components/StepConverterOutputs.tsx'
      to: 'frontend/src/app/features/tools/step-converter/workers/stepConverter.worker.ts'
      via: 'DONE payload warnings'
      pattern: 'conversionWarnings'
---

<objective>
Human verification of Phase 2 performance baseline and triangle-explosion policy using the dev overlay and the browser step converter outputs.

Purpose: Confirm smooth interaction targets and that mitigation/warnings behave as intended on the target class of device.
Output: Verified Phase 2 observable truths; record findings in summary.
</objective>

<execution_context>
@/Users/ilker/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ilker/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-mesh-quality-baseline/02-CONTEXT.md

@frontend/src/app/app.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Mesh quality baseline safeguards (retry-on-explosion + persisted warnings) and dev FPS/debug overlay</what-built>
  <how-to-verify>
    1. Run the app: `npx nx serve @bunlar/frontend`
    2. Open `http://localhost:5173/subjects/climate-tech/assembly-hierarchy-explorer`
    3. Load the sample model (or convert a representative STEP/IGES you have) and orbit/pan/zoom continuously for ~10 seconds.
       Expected:
       - The dev overlay is visible (dev build only).
       - FPS stays near the Phase 2 target (45fps typical, 30fps minimum) most of the time on the baseline device.
       - Controls feel responsive (no noticeable input lag).

    4. Open `http://localhost:5173/tools/step-converter-browser`
    5. Convert a STEP file that is known to be “heavy” (or any representative assembly).
       Expected:
       - Conversion completes and produces a `{base}.zip` download.
       - If the model triggers the explosion threshold, a warning appears only after success (in the outputs panel) and the download is still available.

    6. Unzip the bundle and open `{base}.metadata.json`.
       Expected:
       - It contains `conversionWarnings` (when mitigation occurred) and `meshStats`.

    7. Optional (more strict): verify GLB also contains the warnings in `asset.extras` via a quick node script:
       - `node -e "const fs=require('fs'); const b=fs.readFileSync('<path-to-glb>'); const u=new Uint8Array(b.buffer,b.byteOffset,b.byteLength); const dv=new DataView(u.buffer,u.byteOffset,u.byteLength); const JSON_CHUNK=0x4e4f534a; let off=12; while(off+8<=u.byteLength){const len=dv.getUint32(off,true); const t=dv.getUint32(off+4,true); const s=off+8; const e=s+len; if(e>u.byteLength)break; if(t===JSON_CHUNK){const txt=new TextDecoder().decode(u.subarray(s,e)); const gltf=JSON.parse(txt); console.log(gltf.asset?.extras?.bunlarStepConverter?.conversionWarnings?.length ?? 0); break;} off=e;}"`

  </how-to-verify>
  <resume-signal>Type "approved" or describe what failed (FPS, warnings, persistence, etc.).</resume-signal>
</task>

</tasks>

<verification>
- This plan is complete only after the human verification checkpoint is approved.
</verification>

<success_criteria>

- FPS/debug overlay is visible in dev and reports meaningful stats.
- Step converter emits post-conversion warnings (when needed) and persists them in outputs.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-mesh-quality-baseline/02-04-SUMMARY.md`
</output>
