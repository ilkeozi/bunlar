---
phase: 02-mesh-quality-baseline
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/features/climate-tech/assembly-viewer/visualizer/AssemblyCanvas.tsx
  - frontend/src/app/features/climate-tech/assembly-viewer/components/DevPerfOverlay.tsx
autonomous: true

must_haves:
  truths:
    - 'In dev builds, the Assembly Hierarchy Explorer shows an on-screen FPS/debug overlay'
    - 'Overlay reports FPS and renderer load (triangles, calls) while orbiting'
  artifacts:
    - path: 'frontend/src/app/features/climate-tech/assembly-viewer/components/DevPerfOverlay.tsx'
      provides: 'Dev-only performance overlay'
    - path: 'frontend/src/app/features/climate-tech/assembly-viewer/visualizer/AssemblyCanvas.tsx'
      provides: 'Overlay mounted in the Explorer canvas'
  key_links:
    - from: 'frontend/src/app/features/climate-tech/assembly-viewer/visualizer/AssemblyCanvas.tsx'
      to: 'frontend/src/app/features/climate-tech/assembly-viewer/components/DevPerfOverlay.tsx'
      via: 'React component import'
      pattern: 'DevPerfOverlay'
---

<objective>
Add a dev-only on-screen performance overlay to the Assembly Hierarchy Explorer canvas.

Purpose: Make FPS and renderer load measurable during orbit/select interactions to validate Phase 2 performance targets.
Output: Dev-only overlay component wired into the Explorer canvas.
</objective>

<execution_context>
@/Users/ilker/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ilker/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mesh-quality-baseline/02-CONTEXT.md
@frontend/src/app/features/climate-tech/assembly-viewer/visualizer/AssemblyCanvas.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DevPerfOverlay (FPS + renderer.info) with throttled updates</name>
  <files>frontend/src/app/features/climate-tech/assembly-viewer/components/DevPerfOverlay.tsx</files>
  <action>
Create a small overlay component that reads R3F renderer stats and estimates FPS.

- Implement `DevPerfOverlay` as a React component used inside `<Canvas>`.
- Use `@react-three/fiber` `useThree()` to access `gl` and `useFrame()` to accumulate frames/time.
- Compute FPS as `frames / seconds` over a sliding window, and update React state no more than 4 times per second (e.g. every 250ms) to avoid UI churn.
- Display a compact overlay (top-left or top-right) showing at minimum:
  - FPS
  - `gl.info.render.triangles`
  - `gl.info.render.calls`
- Ensure the overlay does not steal pointer events (`pointer-events: none`) and uses readable contrast.
  </action>
  <verify>
  npx nx run @bunlar/frontend:build
  </verify>
  <done>
  `DevPerfOverlay` compiles and can be mounted in a Canvas without affecting controls.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Mount overlay in AssemblyCanvas for dev builds only</name>
  <files>frontend/src/app/features/climate-tech/assembly-viewer/visualizer/AssemblyCanvas.tsx</files>
  <action>
Wire the overlay into the explorer canvas:

- Import and render `DevPerfOverlay` inside the `<Canvas>` tree.
- Guard rendering with `import.meta.env.DEV` so it is dev-only.
- Keep overlay visually unobtrusive; do not change existing canvas camera/controls behavior.
  </action>
  <verify>
  npx nx run @bunlar/frontend:build
  </verify>
  <done>
  In dev builds, the overlay renders; in production builds it is omitted.
  </done>
  </task>

</tasks>

<verification>
- `npx nx run @bunlar/frontend:build`
</verification>

<success_criteria>

- Dev overlay exists and reports FPS/triangles/calls while orbiting.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-mesh-quality-baseline/02-03-SUMMARY.md`
</output>
